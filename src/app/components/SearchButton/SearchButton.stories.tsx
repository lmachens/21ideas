import React from 'react';

import Button from './SearchButton';

export default {
  title: 'Component/SearchButton',
  component: Button,
};

export const Search = (): JSX.Element => <Button>Search</Button>;
